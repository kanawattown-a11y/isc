# الدليل الشامل: نشر مشروعك باستخدام Supabase + Vercel + Cloudflare

بما أن لديك حسابات على هذه المنصات، إليك الخطوات المحددة جداً لربطها ببعضها:

## أولاً: قاعدة البيانات (Supabase) 🗄️
نحتاج لقاعدة البيانات أولاً لنحصل على الرابط الذي سنضعه في Vercel.

1.  سجل الدخول إلى [Supabase](https://supabase.com/).
2.  اضغط **New Project**.
3.  املأ البيانات (الاسم: `jabal-bashan`، وكلمة مرور قوية **احفظها جيداً**).
4.  انتظر حتى يتم إنشاء المشروع (يأخذ دقيقة).
5.  بعد الانتهاء، اذهب إلى **Project Settings** (أيقونة الترس في الأسفل) -> **Database**.
6.  انزل لأسفل إلى قسم **Connection Pooling** (مهم جداً لـ Vercel).
7.  تأكد أن **Mode** هو `Transaction`.
8.  انسخ الرابط (Connection string). سيكون شكله تقريباً هكذا:
    `postgres://postgres.xxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres`
    *(استبدل `[YOUR-PASSWORD]` بكلمة المرور التي وضعتها).*

## ثانياً: رفع الكود (GitHub) 🐙
1.  افتح الطرفية (Terminal) في مشروعك (VS Code).
2.  نفذ الأوامر التالية لرفع الكود:
    ```bash
    git add .
    git commit -m "Prepare for deployment"
    git branch -M main
    # إذا لم تكن قد ربطت المستودع بعد، قم بإنشاء واحد جديد على GitHub ونفذ أمر الربط الذي سيظهر لك
    git push -u origin main
    ```

## ثالثاً: الباك اند (Vercel) ▲
هنا سننشر "العقل" المدبر للموقع (السيرفر).

1.  سجل الدخول إلى [Vercel](https://vercel.com/dashboard).
2.  اضغط **Add New...** -> **Project**.
3.  اختر مستودع `jabal-bashan` من قائمة GitHub واضغط **Import**.
4.  في صفحة الإعدادات (Configure Project):
    *   **Framework Preset**: اتركه `Other` أو `Vite`.
    *   **Build Command**: بما أننا نريد فقط الـ API هنا، يمكنك تركه كما هو (`npm run build`)، أو لتوفير الوقت في المستقبل يمكنك تجاوزه، لكن اتركه الآن للأمان.
    *   **Environment Variables** (مهم جداً):
        *   أضف متغير باسم `DATABASE_URL` وألصق رابط Supabase الذي نسخته في الخطوة الأولى.
        *   أضف متغير باسم `SESSION_SECRET` واكتب أي نص سري طويل.
5.  اضغط **Deploy**.
6.  بعد النجاح، سيظهر لك رابط (Domains)، مثلاً: `https://jabal-bashan-xyz.vercel.app`.
    *   **انسخ هذا الرابط**، هذا هو رابط الباك اند الخاص بك.

## رابعاً: الفرونت اند (Cloudflare Pages) ☁️
هنا سننشر "واجهة" الموقع التي سيراها الناس.

1.  سجل الدخول إلى [Cloudflare Dashboard](https://dash.cloudflare.com/).
2.  اذهب إلى **Workers & Pages** -> **Create Application** -> **Pages** -> **Connect to Git**.
3.  اختر مستودع `jabal-bashan`.
4.  **Set up builds and deployments**:
    *   **Framework preset**: اختر `Create React App` أو `Vite` (أو اتركها None ولكن تأكد من الإعدادات التالية).
    *   **Build command**: `npm run build`
    *   **Build output directory**: `dist/public`  <-- (تأكد من هذا بدقة).
5.  **Environment variables**:
    *   أضف متغير باسم `VITE_API_URL`.
    *   قيمته هي رابط Vercel الذي نسخته في الخطوة السابقة (مثلاً: `https://jabal-bashan-xyz.vercel.app`) **بدون شرطة مائلة `/` في النهاية**.
6.  اضغط **Save and Deploy**.

---
## الخلاصة: كيف سيعمل الموقع؟
1.  الزائر يدخل على رابط **Cloudflare** (الواجهة).
2.  عندما يرسل رسالة "اتصل بنا"، الواجهة ترسل الطلب إلى رابط **Vercel** (`VITE_API_URL`).
3.  سيرفر **Vercel** يعالج الطلب ويحفظه في قاعدة بيانات **Supabase** (`DATABASE_URL`).

مبروك! لديك الآن بنية تحتية (Infrastructure) كاملة ومحترفة. 🚀
